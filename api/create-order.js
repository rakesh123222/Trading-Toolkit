const {razor}=require('./_utils');
module.exports=async function(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{const {name,email}=req.body||{};if(!name||!email||!String(email).includes('@'))return res.status(400).json({error:'Name and valid email are required'});const order=await razor('/orders',{method:'POST',body:JSON.stringify({amount:39900,currency:'INR',receipt:`gt_${Date.now()}`,notes:{name:String(name).slice(0,100),email:String(email).slice(0,150),product:'Trading Beginner Toolkit'}})});return res.status(200).json({orderId:order.id,amount:order.amount,currency:order.currency,keyId:process.env.RAZORPAY_KEY_ID})}catch(e){return res.status(500).json({error:e.message})}};
