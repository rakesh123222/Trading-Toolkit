const fs=require('fs');const path=require('path');const {readToken}=require('./_utils');
module.exports=async function(req,res){try{readToken(req.query?.token);const file=path.join(process.cwd(),'private','ebook.pdf');if(!fs.existsSync(file))return res.status(404).send('File unavailable');res.setHeader('Content-Type','application/pdf');res.setHeader('Content-Disposition','attachment; filename="Gain_Yourself_Trading_Beginner_Toolkit.pdf"');res.setHeader('Cache-Control','private, no-store');res.send(fs.readFileSync(file));}catch(e){return res.status(403).send('Invalid or expired download link')}};
